const { User } = require('./user.model');
const { TempUser } = require('./tempUser.model');
const { Image } = require('./image.model');
const { AlbumName } = require('./albumName.model');
const { Album } = require('./album.model');

module.exports = {
    User,
    TempUser,
    Image,
    Album,
    AlbumName
}